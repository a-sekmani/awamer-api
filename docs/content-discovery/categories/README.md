# Categories — Index

Public discovery endpoint for content categories. No admin CRUD yet
(see the doc for the manual-flush workaround).

| File | Purpose |
|------|---------|
| [list-categories.md](./list-categories.md) | `GET /api/v1/categories` — active categories with published path/course counts, cache-aside |
